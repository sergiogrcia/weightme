# Fuentes

Coloca aquí los archivos de la familia Inter cuando se incorporen al proyecto.

Después, declara los archivos en `pubspec.yaml` bajo `flutter > fonts` con la
familia `Inter`, que ya está configurada en `lib/theme/app_typography.dart`.
